{
  "name": "election-sitrep",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix sitrep-app\"",
    "build": "npm run build --prefix sitrep-app",
    "build:web": "npm run build --prefix sitrep-app"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
